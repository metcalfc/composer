# composer

Just a dab will do you.

```
 ./composer.py -h
usage: composer.py [-h] [--file FILE] [--outfile OUTFILE]

Convert compose images references to full sha digests

optional arguments:
  -h, --help         show this help message and exit
  --file FILE        Compose File Input (default: ./docker-compose.yml)
  --outfile OUTFILE  Compose File Output (default: ./dab.yml)
```
